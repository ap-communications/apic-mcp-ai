{
  "openapi": "3.0.1",
  "info": {
    "title": "${title}",
    "description": "${description}",
    "version": "1.0"
  },
  "servers": [
    {
      "url": ""
    }
  ],
  "paths": {
    "/test": {
      "post": {
        "summary": "test Endpoint",
        "description": "test endpoint. ${title}のダミーオペレーション。(API実行不可)",
        "operationId": "test-endpoint",
        "responses": {
          "200": {
            "description": ""
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "apiKeyHeader": {
        "type": "apiKey",
        "name": "Ocp-Apim-Subscription-Key",
        "in": "header"
      },
      "apiKeyQuery": {
        "type": "apiKey",
        "name": "subscription-key",
        "in": "query"
      }
    }
  },
  "security": [
    {
      "apiKeyHeader": []
    },
    {
      "apiKeyQuery": []
    }
  ]
}
